module main( in op(1),out c[2](4),inout a[2](4), inout b[2](4))

if op then
 for $i = 0 to 1 do  
    c[$i] ^= (a[$i] + b[$i]);
  rof;
else
 for $i = 0 to 1 do  
    c[$i] ^= (a[$i] - b[$i]);
 rof;
fi op
