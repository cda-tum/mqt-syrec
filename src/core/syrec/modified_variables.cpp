#include "core/syrec/modified_variables.hpp"

namespace revkit::syrec {

        [[maybe_unused]] void modified_variables(const program& prog [[maybe_unused]],
                                std::map<statement::ptr, std::set<variable::ptr>>&) {
        }

    } // namespace revkit
