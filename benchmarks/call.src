module call_func( in x(1), inout y(32), inout z(32), inout t(32))
  if x
    then
      y += (z+t)
    else
      z -= (y-t)
  fi x

module main( in a(1), inout x(32), inout y(32),inout s(32) )
  call call_func( a, x, y,s )
